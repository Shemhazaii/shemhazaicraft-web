apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: minecraft
  labels:
    app: shemhazaicraft
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: shemhazaicraft
    release: {{ .Release.Name }}